package com.parkit.app.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors the palette already established in the web demo / changelog artifacts
// (parkit-changelog.html, parkit-roadmap.html) so the brand is consistent
// across every surface, not just this app.
object ParkItColors {
    val AccentLight = Color(0xFF1B4F91)
    val AccentDark = Color(0xFF6AA3E0)

    val BgLight = Color(0xFFF3F4F7)
    val SurfaceLight = Color(0xFFFFFFFF)
    val SurfaceTintLight = Color(0xFFEAF1FA)
    val TextLight = Color(0xFF1A1F29)
    val TextMutedLight = Color(0xFF5B6472)
    val BorderLight = Color(0xFFDEE2E8)

    val BgDark = Color(0xFF12161D)
    val SurfaceDark = Color(0xFF1A2029)
    val SurfaceTintDark = Color(0xFF16283B)
    val TextDark = Color(0xFFE8EAEE)
    val TextMutedDark = Color(0xFF97A1AF)
    val BorderDark = Color(0xFF2A313D)

    val Done = Color(0xFF2C7A4B)
    val DoneDark = Color(0xFF6FCB9A)
    val Warn = Color(0xFFB8631A)
    val WarnDark = Color(0xFFE0964C)
}
